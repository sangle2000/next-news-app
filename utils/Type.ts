export type newsType = {
    id: string,
    slug: string,
    title: string,
    image: string,
    date: string,
    content: string
}

export type yearType = {
    year: string
}

export type monthType = {
    month: string
}